# Test dependencies for non-Nix environments.
# Nix users should use `nix develop`; the flake provides these dependencies.
pytest>=3.7.3
pytest-regtest>=1.3.1
tzlocal>=1.5.1
pytz>=2018.5
pytest-benchmark>=3.2.3
